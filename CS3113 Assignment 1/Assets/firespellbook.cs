using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class firespellbook : MonoBehaviour
{
    // Start is called before the first frame update

    Animator sbAni;

    void Start()
    {
        sbAni = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
